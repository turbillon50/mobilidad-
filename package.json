{
  "name": "mobilidad-platform",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "cd apps/api && npm run db:migrate",
    "db:seed": "cd apps/api && npm run db:seed"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  },
  "packageManager": "npm@10.9.7",
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "@clerk/nextjs": "5.7.6"
  }
}
